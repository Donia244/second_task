on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "login to Docker hup"
        run: docker login --username donia2002 --password dckr_pat_C_kv4iP1mVXSCOCnAGgX4bNgZ9I


